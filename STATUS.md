# Release status

Current status: **Candidate — `E2E` tutorial source complete, corpus pinned and licence cleared; execution outstanding**. The pipeline package with its adaptation contract (`predict`, `evaluate`, `adapt`, `save_artifact`, `load_artifact` / `from_artifact`), the carried `metrics.py` (accuracy with unmatched answers counted wrong, chance, the position-prior and longest-option baselines) and `samples.py` (one AI2D test shard from `lmms-lab-encoder/ai2d`, split by diagram), the offline unit suites (pipeline, adaptation, role-helper, import-boundary and notebook-parity) and the model-backed adaptation suite on a small random Pix2Struct built offline from the committed config, VQA processor and tokenizer, `MODEL_CARD.md` (MODEL_CARD_SPEC 1.1), and the standalone `E2E` tutorial `tutorials/pix2struct_ai2d_colab.ipynb` (generated by `tools/build_notebook.py`, DIMER Notebook Specification 2.0 §4) exist and pass static validation (`tools/validate_release_assets.py`, including the generator parity checks PAR1–PAR4). The shard's SHA-256 pin is recorded in `samples.py` (`tools/pin_corpus.py`; 1,544 rows), and AI2D's licence terms were reviewed and cleared for this use by the maintainer (Kurt Valcorza, 2026-09-26); the Hub mirror itself still declares no licence. The fine-tuning recipe has not been run on the real checkpoint, so the notebook records `frozen_beats_chance` and `adapted_beats_frozen` instead of asserting them. The Kaggle and local runs in `docs/release-verification.md` were of the previous `TASK-INFERENCE` notebook and are not evidence for this revision; the repository is not release-grade until a Colab/Kaggle execution for the exact candidate blob is recorded there.
