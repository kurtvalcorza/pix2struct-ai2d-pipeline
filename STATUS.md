# Release status

Current status: **Release-grade — exact-blob `E2E` run passed on Kaggle Tesla T4** (commit `75255e0`, notebook blob `02cd3fb8fda4`, `kurtvalcorza/dimer-nb2-pix2struct-ai2d` v3, 2026-09-26 UTC, 1173.5 s, 11/11 code cells after the expected restart following the install cell). The pipeline package with its adaptation contract (`predict`, `evaluate`, `adapt`, `save_artifact`, `load_artifact` / `from_artifact`), the carried `metrics.py` (accuracy with unmatched answers counted wrong, chance, the position-prior and longest-option baselines) and `samples.py` (one AI2D test shard from `lmms-lab-encoder/ai2d`, digest-pinned and split by diagram), the offline unit suites, `MODEL_CARD.md` (MODEL_CARD_SPEC 1.1), and the standalone `E2E` tutorial `tutorials/pix2struct_ai2d_colab.ipynb` (generated by `tools/build_notebook.py`, DIMER Notebook Specification 2.0 §4) pass static validation, and the exact notebook blob ran top-to-bottom in a clean runtime. On the default seeded split (360 / 82 / 161 questions on whole diagrams) the held-out test accuracy was 0.354 for the frozen model and 0.366 after adapting the last two decoder blocks, against chance 0.25, the position-prior baseline 0.193 and the longest-option baseline 0.248; the unmatched rate went from 0.075 to 0.068, and the adapter reloaded with 8/8 identical answers. These are measurements of one seeded split of AI2D test questions on one runtime with no dispersion estimate, not an AI2D benchmark, and the +0.012 gain is two questions. The shard's SHA-256 pin is recorded in `samples.py` (`tools/pin_corpus.py`; 1,544 rows), and AI2D's licence terms were reviewed and cleared for this use by the maintainer (Kurt Valcorza, 2026-09-26); the Hub mirror itself still declares no licence. Still open for the maintainer: whether to restore a gain assertion in the notebook now that the recipe has one measurement. The full record, runtime identity and output digests are in `docs/release-verification.md`; the first T4 attempt on `4692451` failed on an embedded-module `_hub_download` name collision, fixed in `75255e0`. Any change to the notebook blob returns the carrier to Candidate until a new exact-blob run is recorded.
